---
name: Compile Firmware
description: Compile the Centipede firmware.
---

When asked to "Compile the firmware", execute the following command:

```bash
cd v1/firmware && ./build.sh
```

The firmware will be compiled to `v1/firmware/build/centipede.uf2`.
