---
name: Compile Tether
description: Compile the tether program.
---

When asked to "Compile the tether", execute the following command:

```bash
cd v1/tether && ./build.sh
```

The tether program will be compiled to `v1/tether/build/tether`.
